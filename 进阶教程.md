# 【提分教程】赛题三：稀疏反馈下的自动化实验挑战

## 附：Baseline源代码文件

## 赛题核心分析

### 赛事背景

在真实的金融模型研发过程中，实验优化往往不是一次性完成的。研究者面临四大约束：

| 约束 | 含义 | 对 Agent 的要求 |
| --- | --- | --- |
| **有限预算** | 只能运行有限次数实验 | 精打细算，避免无效尝试 |
| **稀疏反馈** | 只能看到 `val_acc`、`val_ndcg` 等间接指标 | 从有限信号中提炼规律 |
| **禁止并行** | 每次只能跑一个实验 | 串行决策，不能暴力搜索 |
| **目标未知** | 不知道全局最优配置 | 逐步逼近，善用早停 |

## 问题本质：POMDP

从控制论视角，这是一个**部分可观察马尔可夫决策过程**：

```text
状态空间  ->  当前模型结构、超参数、训练策略
观察空间  ->  val_acc、val_ndcg、训练耗时、错误日志
动作空间  ->  调 lr、换模型、改 hidden_dim、早停
奖励函数  ->  最终测试集 Accuracy / NDCG@10
约束条件  ->  budget 上限、时间限制、禁止并行
```

## 技术建模框架

```text
┌──────────────────────────────────────────────────────────────┐
│                       自主科研 Agent 系统                     │
├──────────────┬──────────────┬──────────────┬──────────────┤
│ Literature   │ Diagnosis    │ Design       │ Experiment   │
│ 文献解析      │ 瓶颈诊断      │ 代码设计      │ 实验验证       │
├──────────────┼──────────────┼──────────────┼──────────────┤
│ 读取README    │ 分析历史记录   │ LLM生成代码   │ 运行 train.py  │
│ 探查数据分布   │ 识别瓶颈      │ 语法验证      │ 读取 metrics   │
│ 分析现有代码   │ 提出假设      │ Smoke Test   │ CONTINUE/PIVOT │
│              │              │ 保存修改      │ /STOP         │
└──────────────┴──────────────┴──────────────┴──────────────┘
                              ↑───────────────────────────────┘
                              迭代闭环（预算内循环）
```

## 任务详解

### Task 1：产品分类（图节点分类）

**输入：** `data/cls_data/A1.npz`

```python
{
    "adj_data": [...],       # 邻接矩阵 CSR data
    "adj_indices": [...],    # 邻接矩阵 CSR indices
    "adj_indptr": [...],     # 邻接矩阵 CSR indptr
    "adj_shape": [13752, 13752],  # 节点数
    "attr_data": [...],      # 特征矩阵 CSR data
    "attr_indices": [...],
    "attr_indptr": [...],
    "attr_shape": [13752, 767],   # 节点数 × 特征维度
    "labels": [...],         # 节点标签，测试节点为 -1
    "train_idx": [...],      # 训练节点索引
    "test_idx": [...]        # 测试节点索引
}
```

**A 榜数据规模：**

| 指标 | 数值 |
| --- | --- |
| 节点数 | 13,752 |
| 特征维度 | 767 |
| 类别数 | 10 |
| 训练节点 | 11,001 |
| 测试节点 | 2,751 |

**输出格式：** `A1.csv`

```csv
test_idx,label
0,3
3,7
6,2
...
```

**评测指标：** Accuracy（测试节点预测准确率）

### Task 2：产品推荐（序列 Top-K 推荐）

**输入文件：**

| 文件 | 说明 |
| --- | --- |
| `train.csv` | 训练集：uid, target_iid, item_seq_raw, item_seq_dedup, item_seq_counts |
| `test.csv` | 测试集：uid, item_seq_raw, item_seq_dedup, item_seq_counts |
| `user.csv` | 用户匿名特征 |
| `item.csv` | 物品匿名特征，候选物品集合 |

**A 榜数据规模：**

| 指标 | 数值 |
| --- | --- |
| 总用户数 | 50,000 |
| 训练用户数 | 40,000 |
| 测试用户数 | 10,000 |
| 物品数 | 2,156 |

**输出格式：** `A2.csv`

```csv
uid,prediction
u000009,"i000481,i000909,i001952,i001069,i000661,i001663,i001922,i000695,i001673,i000521"
...
```

**评测指标：** NDCG@10

```text
DCG@10 = Σ (2^rel_i - 1) / log2(i + 1)
NDCG@10 = DCG@10 / IDCG@10
```

其中 `rel_i = 1` 表示第 i 位是目标 item。

## 最终得分

```text
Score_final = 0.5 × Score_cls + 0.5 × Score_ndcg
```

> 两个任务同等重要，不能只优化一个。

## Baseline 方案思路

### 模型选择

**Task 1 默认：GraphSAGE**

| 模型 | 优点 | 缺点 | 适用场景 |
| --- | --- | --- | --- |
| GCN | 简单高效 | 对稀疏图效果一般 | 图较密集 |
| **SAGE** | 均值聚合，对稀疏图鲁棒 | 表达能力中等 | **金融稀疏图（默认）** |
| GAT | 注意力机制，表达能力强 | 计算开销大，易过拟合 | 有充足预算时 |

**Task 2 默认：GRU4Rec**

| 模型 | 优点 | 缺点 | 适用场景 |
| --- | --- | --- | --- |
| **GRU4Rec** | 参数量小、训练快 | 长程依赖弱 | **预算有限（默认）** |
| SASRec | Transformer，表达能力强 | 需要多数据和训练时间 | 预算充足 |

## 四阶段优化流程

```text
第 1 轮（冷启动）
  Literature  ->  读取数据 + 分析代码
      ↓
  Diagnosis   ->  识别瓶颈，提出 3 个初始假设
      ↓
  Design      ->  LLM 生成代码修改（如增大 hidden_dim）
      ↓
  Experiment  ->  训练 + 推理 -> val_acc/val_ndcg
      ↓
  Memory 记录 <- 保存 config + metrics

第 2~N 轮（迭代优化）
  Diagnosis   ->  读取历史记录，判断哪个方向有效
      ↓
  Design      ->  聚焦最有效的假设继续优化
      ↓
  Experiment  ->  训练 + 推理
      ↓
  Decision    ->  CONTINUE / PIVOT / STOP
```

## 核心代码解读

### 模型定义（code/models.py）

**GraphSAGE 层：**

```python
class SAGELayer(nn.Module):
    def __init__(self, in_dim, out_dim, aggr='mean'):
        super().__init__()
        self.aggr = aggr
        self.linear = nn.Linear(in_dim * 2, out_dim)

    def forward(self, x, adj):
        # 聚合邻居：adj @ x -> (N, in_dim)
        h_neigh = adj @ x
        if self.aggr == 'mean':
            degree = adj.sum(dim=1, keepdim=True).clamp(min=1)
            h_neigh = h_neigh / degree

        # 拼接自身和邻居
        h_concat = torch.cat([x, h_neigh], dim=-1)
        return F.relu(self.linear(h_concat))
```

关键点：使用 `adj @ x` 而不是构造三维张量，避免 OOM。

**GRU4Rec 模型：**

```python
class GRU4Rec(nn.Module):
    def __init__(self, num_items, embedding_dim=64, hidden_dim=128,
                 num_layers=1, dropout=0.2, max_len=50):
        super().__init__()
        self.item_embedding = nn.Embedding(num_items + 1, embedding_dim, padding_idx=0)
        self.gru = nn.GRU(embedding_dim, hidden_dim, num_layers,
                          batch_first=True, dropout=dropout if num_layers > 1 else 0)
        self.output_projection = nn.Linear(hidden_dim, num_items + 1)
```

### 训练流程（code/train.py）

**Task 1 训练核心：**

```python
# 全图训练
for epoch in range(1, args.epochs + 1):
    logits = model(features, adj)
    loss = criterion(logits[train_idx_t], labels[train_idx_t])
    loss.backward()
    torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=5.0)
    optimizer.step()

    # 验证
    val_acc = compute_accuracy(logits[val_idx_t], labels[val_idx_t])

    # 保存最佳模型
    if val_acc > best_val_acc:
        torch.save({...}, best_path)
```

**Task 2 训练核心：**

```python
# 序列推荐，使用 BPR 或 CE 损失
for batch_seq, batch_target, batch_len in train_loader:
    logits = model(batch_seq, batch_len)

    # BPR 损失：正样本得分 > 负样本得分
    if args.loss_type == 'bpr':
        loss = bpr_loss(pos_score, neg_score)
    else:
        loss = criterion(logits, batch_target)
```

### Agent 编排（agent/orchestrator.py）

```python
class ResearchOrchestrator:
    def run_task(self, task_id):
        # 1. 设置任务专属输出目录
        task_output_dir = os.path.join(self.output_dir, f"task{task_id}")
        task_config.output_dir = task_output_dir

        # 2. Literature（仅一次）
        lit_phase = LiteraturePhase(...)
        lit_result = lit_phase.run()

        # 3. 迭代闭环
        while decision in (CONTINUE, PIVOT):
            diagnosis = DiagnosisPhase(...).run()
            design = DesignPhase(...).run()
            experiment = ExperimentPhase(...).run(iteration)
            decision = experiment["decision"]

        # 4. 打包提交
        return self._finalize(task_id)
```

## 提分技巧

### Task 1 提分方向

1. **模型架构尝试**

```yaml
task1:
  model_type: "gat"     # 从 sage 换到 gat，可能提升 1~3%
  hidden_dim: 256       # 增大模型容量
  num_layers: 3         # 增加感受野
```

2. **数据增强**

- DropEdge：随机丢弃部分边，增强鲁棒性
- Feature Masking：随机掩码部分特征
- 对称归一化 vs 随机游走归一化

3. **训练策略**

- 调整学习率（0.01 → 0.005 → 0.001）
- 使用 Weight Decay（5e-4）
- 分层划分训练/验证集（`--stratified_split`）

### Task 2 提分方向

1. **序列建模升级**

- 从 GRU4Rec 升级到 SASRec
- 增加 max_seq_len（50 → 100）
- 增加 embedding_dim（64 → 128）

2. **损失函数优化**

- BPR 损失 → CE 损失
- 增加负采样数量（1 → 5）

3. **后处理策略**

- 去重：避免推荐列表重复
- 多样性：加入热门惩罚
- 用户画像融合：将 user.csv 特征融入模型

## Agent 策略优化

1. **预算分配：** 如果 Task 1 已收敛，将预算转移给 Task 2
2. **失败快速恢复：** 训练失败时回滚到上一个稳定配置
3. **贝叶斯优化：** 用 Optuna 替代 LLM 做超参数搜索
4. **跨任务迁移：** 从 Task 1 学到的经验迁移到 Task 2

## 优化输入数据质量

### 特征工程

```python
# 示例：对稀疏特征矩阵做行归一化
from sklearn.preprocessing import normalize

features_normalized = normalize(features, norm='l2', axis=1)
```

### 图结构增强

```python
# DropEdge：随机丢弃 10% 的边
def drop_edge(adj, drop_rate=0.1):
    mask = torch.rand(adj.shape) > drop_rate
    return adj * mask
```

### 序列预处理

```python
# 对交互序列做去重和截断
def preprocess_seq(seq, max_len=50):
    seq = list(dict.fromkeys(seq))  # 去重，保留顺序
    return seq[-max_len:]           # 取最近 max_len 个
```

## 优化处理流程

### 减少训练时间

1. **增大 batch_size：** 256 → 512（需要多显存/内存）
2. **使用 GPU：** `--device cuda`
3. **早停耐心值：** patience 从 20 → 10

### 提高实验效率

1. **预筛选配置：** 在 Diagnosis 阶段同时生成 3 个假设，按优先级串行验证
2. **缓存模型：** 保存每轮 best_model，避免重复训练

## 附录：补充知识

### A. CSR 稀疏矩阵

CSR（Compressed Sparse Row）格式只存储非零元素，适合大规模稀疏图：

```python
import scipy.sparse as sp

# 从 .npz 还原
adj = sp.csr_matrix(
    (data['adj_data'], data['adj_indices'], data['adj_indptr']),
    shape=tuple(data['adj_shape'])
)

# 矩阵乘法
h_neigh = adj @ x  # 高效，时间复杂度 O(边数)
```

### B. GraphSAGE vs GCN

| 特性 | GCN | GraphSAGE |
| --- | --- | --- |
| 聚合方式 | 谱域卷积 | 采样 + 聚合 |
| 可扩展性 | 差（需要全图） | 好（可 mini-batch） |
| 对稀疏图 | 一般 | 鲁棒 |
| 参数量 | 少 | 略多 |

### C. NDCG@10

```text
DCG@10 = Σ_{i=1}^{10} (2^{rel_i} - 1) / log2(i + 1)
NDCG@10 = DCG@10 / IDCG@10
```

`rel_i = 1` 当且仅当第 i 位是目标 item。

### D. BPR 损失

```text
L_BPR = -Σ ln(σ(pos_score - neg_score))
```

让正样本得分高于负样本得分。

## 参考资料

1. Hamilton, W. L., et al. "Inductive representation learning on large graphs." *NeurIPS 2017.*（GraphSAGE）
2. Kipf, T. N., & Welling, M. "Semi-supervised classification with graph convolutional networks." *ICLR 2017.*（GCN）
3. Hidasi, B., et al. "Session-based recommendations with recurrent neural networks." *ICLR 2016.*（GRU4Rec）
4. Kang, W. C., & McAuley, J. "Self-attentive sequential recommendation." *ICDM 2018.*（SASRec）
5. PyTorch 官方文档：https://pytorch.org/docs/
6. SciPy 稀疏矩阵文档：https://docs.scipy.org/doc/scipy/reference/sparse.html
