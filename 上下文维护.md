# 上下文维护文档

本文档用于记录当前比赛项目的代码、数据、结果和提交版本，避免后续提交时选错文件。

## 当前项目上下文

- 比赛任务：AFAC2026 挑战组赛题三，稀疏反馈下的自动化实验挑战。
- 当前阶段：A 榜预测结果生成。
- 本地 A 榜分类数据：`A分类/A1.npz`
- 本地 A 榜分类模板：`A分类/sample_submission.csv`
- 本地 A 榜推荐数据：`A推荐/`
- Qwen API 配置：根目录 `.env`
- PyTorch 环境：`E:\desktop\Artificial-Intelligence\Pytorch\.venv\Scripts\python.exe`
  - 已验证：`torch 2.11.0+cu130`
  - CUDA：可用，设备为 `NVIDIA GeForce RTX 4060 Laptop GPU`
- 主方案目录：`our_solution/`
- 主运行入口：`our_solution/run.py`
- 分数台账：`分数记录.md`

## 提交文件规则

A 榜提交时，优先使用根目录最新版：

```text
prediction.zip
```

该文件由 `our_solution/run.py` 每次运行后自动更新，是当前最新可提交文件。

版本备份位于：

```text
versions/vN/prediction.zip
```

如果需要回滚或复查历史结果，请从对应版本目录中查找。

## 版本目录约定

每个版本目录包含：

```text
versions/vN/
  prediction.zip
  submission/
    A1.csv
    A2.csv
    trajectory_B1.json
    trajectory_B2.json
  code/
    run.py
    README.md
    requirements.txt
    config.example.yaml
    src/
  run_summary.json
  classification_result.json
  recommendation_result.json
  version_summary.json
```

跨版本索引：

```text
versions/VERSION_INDEX.json
```

说明：

- `prediction.zip`：该版本可提交压缩包。
- `submission/`：该版本解压后的 CSV 和过程日志。
- `code/`：该版本对应的代码快照。
- `version_summary.json`：该版本的运行参数、指标摘要和文件位置。
- `VERSION_INDEX.json`：所有版本的结构化汇总，包含内部指标和当前最佳版本。

## 运行命令

生成指定版本，例如 v1：

```powershell
python .\our_solution\run.py --budget 5 --seed 42 --version v1 --note "A榜初版离线方案"
```

自动生成下一个版本：

```powershell
python .\our_solution\run.py --budget 5 --seed 42
```

指定 B 榜数据时：

```powershell
python .\our_solution\run.py `
  --cls_data <B1.npz> `
  --cls_sample <B1_sample_submission.csv> `
  --rec_data <B2_rec_data_dir> `
  --output_dir .\our_solution\output_b `
  --budget 5 `
  --version v2
```

## 当前版本记录

### 最佳版本

当前根目录提交包：`v17`

官方 A 榜已验证最佳版本：`v17`

内部验证最高版本：`v15`（已线上验证失效，不能作为提交依据）

当前可提交候选：`v17`（已验证当前最佳）

判断依据：

- v17 官方 A 榜提交时间：`2026-06-17 16:58:13`
- v17 官方 A 榜分数：总分 `0.6266`，分类 `0.7586`，推荐 `0.4947`，排名 `25`
- v17 分类复用 v8，官方 A 榜分类参考分 `0.7586`
- v17 推荐在 v16 基础上只对 `seq_len=1/2/3` 做保守短序列神经 rerank，`seq_len=0` 和 `seq_len>=4` 不变
- v17 推荐测试分布加权 NDCG@10：`0.479688`，相比 v16 的 `0.479332` 单 split 提升 `+0.000356`
- v17 天然短历史验证 NDCG@10：`0.367369`，相比 v16 的 `0.358761` 提升 `+0.008607`
- v17 四 split 稳健探针：相对 v16 平均 test-weighted 提升 `+0.000790`，最小提升 `+0.000356`
- v17 A2 相比 v16 变化 `4194` 行，全部为 `seq_len=1/2/3`；`top1` 变化 `0` 行
- v17 相比 v16：总分 `+0.0001`，分类不变，推荐 `+0.0003`，排名保持 `25`
- v18 probe 未通过提交门槛：零历史 5-seed + v17 短历史组合 5 split 平均不高于 v17，最小 delta 为负；根目录 `prediction.zip` 保持 v17
- v16 官方 A 榜提交时间：`2026-06-17 15:17:55`
- v16 官方 A 榜分数：总分 `0.6265`，分类 `0.7586`，推荐 `0.4944`，排名 `25`
- v16 分类复用 v8，官方 A 榜分类参考分 `0.7586`
- v16 推荐只改 `seq_len=0` 零历史用户，不再触碰 v15 失败的 `seq_len=1/2/3`
- v16 推荐测试分布加权 NDCG@10：`0.479332`，相比 v14 的 `0.479141` 单 split 提升 `+0.000191`
- v16 四 split 零历史稳健探针：相对 v14-like 平均提升 `+0.001506`，最小提升 `+0.000191`
- v16 A2 相比 v14 变化 `3454` 行，全部为 `seq_len=0`
- v16 相比 v14：总分 `+0.0001`，分类不变，推荐 `+0.0002`，排名保持 `25`
- v15 官方 A 榜总分：`0.6255`，分类 `0.7586`，推荐 `0.4923`
- v15 相比 v14：总分 `-0.0009`，分类不变，推荐 `-0.0019`
- v15 的短历史神经塔离线验证提升没有转化到线上，说明当前 masked/test-weighted 验证对 `seq_len=1/2/3` 用户存在明显分布错配
- v15 官方验证失败后曾回退 v14；当前根目录 `prediction.zip` 已更新为 v17 官方最佳版本
- v15 推荐测试分布加权 NDCG@10：`0.481515`，相比 v14 的 `0.479141` 提升 `+0.002374`
- v15 推荐普通内部 NDCG@10：`0.569711`，相比 v14 的 `0.568716` 提升 `+0.000995`
- v15 分类复用 v8，官方 A 榜分类参考分 `0.7586`
- v15 推荐在 v14 零历史 3seed 神经塔 ensemble 基础上，新增 `seq_len=1/2/3` 用户特征+短序列神经塔融合
- v15 A2 相比 v14 变化 `5441` 行：`seq_len=1` 变化 `997/1003`，`seq_len=2` 变化 `49/49`，`seq_len=3` 变化 `4395/4425`
- v15 失败结论：短历史神经塔过拟合内部验证，不再作为后续主线提交；后续短历史改动必须用更严格的线上风险约束
- v14 推荐测试分布加权 NDCG@10：`0.479141`，相比 v12 的 `0.477841` 提升 `+0.001300`
- v14 分类复用 v8，官方 A 榜分类参考分 `0.7586`
- v14 推荐使用零历史 3seed 神经塔 ensemble；A2 相比 v12 变化 `3455` 行，全部为零历史用户
- v14 预期 A 榜分数：分类约 `0.7586`，推荐约 `0.4939`，总分约 `0.62625`
- v14 官方 A 榜总分：`0.6264`，分类 `0.7586`，推荐 `0.4942`，排名 `25`
- v14 相比 v12：总分 `+0.0011`，分类 `+0.0007`，推荐 `+0.0016`，排名从 `27` 提升到 `25`
- v12 内部综合分：`0.658034`（分类复用 v11；推荐普通 NDCG 与 v8 持平，但测试分布加权 NDCG 明显提升）
- v12 分类内部 Accuracy：`0.747352`（复用 v11）
- v12 推荐内部普通 NDCG@10：`0.568716`
- v12 推荐 masked validation NDCG@10：`0.477442`
- v12 推荐测试分布加权 NDCG@10：`0.477841`，相比 v8/v11 的 `0.474728` 提升 `+0.003113`
- v12 A1 与 v11 完全一致；A2 相比 v11 变化 `3515` 行，全部为零历史用户
- v12 官方 A 榜总分：`0.6253`，分类 `0.7579`，推荐 `0.4926`，排名 `27`
- v12 相比 v8：总分 `+0.0014`，分类 `-0.0007`，推荐 `+0.0035`，排名从 `30` 提升到 `27`
- v13 为低风险重组候选：复用 v8 分类 `A1.csv` 与 v12 推荐 `A2.csv`
- v13 理论 A 榜分数：分类 `0.7586`，推荐 `0.4926`，总分约 `0.6256`，预计比 v12 再高 `+0.0003`
- v11 内部综合分：`0.658034`（分类重算；推荐复用官方最佳 v8）
- v11 分类内部 Accuracy：`0.747352`，相比 v8/v10 的 `0.746596` 提升 `+0.000756`
- v11 分类 20 seed 探针：`class_prior_beta=-0.25` 平均提升约 `+0.00189`，20 个 seed 中 11 个为正
- v11 推荐内部普通 NDCG@10：`0.568716`（复用 v8）
- v11 推荐测试分布加权 NDCG@10：`0.474728`（复用 v8）
- v11 A1 相比 v8 变化 `71` 行；A2 与 v8 完全一致
- v11 官方 A 榜总分：待提交回填
- v10 内部综合分：`0.657689`（分类复用 v9/v8；推荐普通 NDCG 与测试分布加权 NDCG 均极小幅高于 v9）
- v10 分类内部 Accuracy：`0.746596`（复用 v9/v8/v7）
- v10 推荐内部普通 NDCG@10：`0.568782`
- v10 推荐测试分布加权 NDCG@10：`0.474768`，相比 v9 提升约 `+0.0000003`
- v10 A1 与 v9 完全一致；A2 相比 v9 变化 `718` 行，其中 `seq_len=1-3` 变化 `712` 行，`seq_len=0` 变化 `6` 行
- v10 多 seed 探针相对 v9 平均提升约 `+0.000041`，8 个 seed 中 6 个为正，但单 seed 收益极小，提交风险高于 v9
- v10 官方 A 榜总分：`0.6239`，分类 `0.7586`，推荐 `0.4891`，排名 `31`
- v10 相比 v8：官方分数无变化；排名下降来自榜单变化，不作为模型改进依据
- v9 内部综合分：`0.657686`（分类复用 v8；推荐普通 NDCG 与测试分布加权 NDCG 均小幅高于 v8）
- v9 分类内部 Accuracy：`0.746596`（复用 v8/v7）
- v9 推荐内部普通 NDCG@10：`0.568777`
- v9 推荐测试分布加权 NDCG@10：`0.474768`，相比 v8 提升 `+0.000040`
- v9 A1 与 v8 完全一致；A2 相比 v8 变化 `113` 行，其中 `seq_len=0` 变化 `64` 行，`seq_len=2` 变化 `49` 行
- v9 官方 A 榜总分：待提交回填
- v8 内部综合分：`0.657656`（分类复用 v7；推荐普通 NDCG 与 v7 持平）
- v8 分类内部 Accuracy：`0.746596`（复用 v7）
- v8 推荐内部普通 NDCG@10：`0.568716`
- v8 推荐测试分布加权 NDCG@10：`0.474728`，相比 v7/v6 提升 `+0.000949`
- v8 A1 与 v7 完全一致；A2 相比 v7 变化 `3424` 行，全部为零历史用户
- v8 官方 A 榜总分：`0.6239`
- v8 官方 A 榜排名：`30`
- v8 官方 A 榜分类分数：`0.7586`
- v8 官方 A 榜推荐分数：`0.4891`
- v8 相比 v7：总分 `+0.0007`，分类不变，推荐 `+0.0013`，排名下降 `1` 名
- v7 内部综合分：`0.657656`
- v7 分类内部 Accuracy：`0.746596`，相比 v6/v5 的 `0.742057` 提升 `+0.004539`
- v7 推荐复用 v6，推荐测试分布加权 NDCG@10：`0.473779`
- v7 A1 相比 v6 变化 `86` 行；A2 与 v6 完全一致
- v7 官方 A 榜总分：`0.6232`
- v7 官方 A 榜排名：`29`
- v7 官方 A 榜分类分数：`0.7586`
- v7 官方 A 榜推荐分数：`0.4878`
- v7 相比 v6：总分 `+0.0024`，分类 `+0.0047`，推荐不变，排名提升 `4` 名
- v6 内部综合分：`0.655387`
- v6 分类内部 Accuracy：`0.742057`（复用 v5）
- v6 推荐内部普通 NDCG@10：`0.568716`
- v6 推荐测试分布加权 NDCG@10：`0.473779`，相比 v5 提升 `+0.001527`
- v6 官方 A 榜总分：`0.6208`
- v6 官方 A 榜排名：`33`
- v6 官方 A 榜分类分数：`0.7539`
- v6 官方 A 榜推荐分数：`0.4878`
- v6 相比 v5：总分 `+0.0012`，分类不变，推荐 `+0.0025`，排名不变
- v5 内部综合分：`0.653347`
- v5 分类内部 Accuracy：`0.742057`，相比 v4/v3 提升 `+0.008321`
- v5 推荐内部普通 NDCG@10：`0.564636`
- v5 推荐测试分布加权 NDCG@10：`0.472251`，相比 v4 提升 `+0.000883`
- v5 官方 A 榜总分：`0.6196`
- v5 官方 A 榜排名：`33`
- v5 官方 A 榜分类分数：`0.7539`
- v5 官方 A 榜推荐分数：`0.4853`
- v5 相比 v4：总分 `+0.0040`，分类 `+0.0054`，推荐 `+0.0025`，排名提升 `4` 名
- v4 官方 A 榜总分：`0.6156`
- v4 官方 A 榜排名：`37`
- v4 官方 A 榜分类分数：`0.7485`
- v4 官方 A 榜推荐分数：`0.4828`
- v4 相比 v3：总分 `+0.0063`，推荐 `+0.0127`，分类不变
- v3 官方 A 榜总分：`0.6093`
- v3 官方 A 榜排名：`45`
- v3 相比 v2：总分 `+0.0273`，分类 `+0.0546`，推荐不变
- v2 复用 v1 分类结果，推荐内部普通 NDCG@10：`0.560433`
- v2 推荐测试分布加权 NDCG@10：`0.455747`
- v2 内部综合分：`0.635360`
- v3 分类内部 Accuracy：`0.733737`
- v3 推荐复用 v2，内部综合分：`0.647085`
- v4 分类复用 v3，推荐测试分布加权 NDCG@10：`0.471369`
- v4 相比 v3 推荐测试分布加权 NDCG@10：`+0.015621`
- 当前已知榜首：总分 `0.63524`，分类 `0.76409`，推荐 `0.50639`
- v17 官方最佳距离已知榜首：总分差 `0.00864`，分类差 `0.00549`，推荐差 `0.01169`。
- v17 相比 v16 只小幅缩小推荐差距；后续若继续提交，需要有比 `+0.0001` 更实质的验证收益。

当前根目录最新版提交文件：

```text
prediction.zip
```

对应备份：

```text
versions/v17/prediction.zip
```

已验证官方最佳备份：

```text
versions/v17/prediction.zip
```

### 版本总览

| 版本 | 日期 | 核心改动 | 分类内部 | 推荐内部 | 内部综合 | A榜分类 | A榜推荐 | A榜总分 | 是否最佳 | 提交文件 |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- |
| v1 | 2026-06-16 | 初版自有方案；离线轻量模型；自动版本归档；根目录最新版 zip | 0.710287 | 0.557205 | 0.633746 | 0.6939 | 0.4687 | 0.5813 | 历史版本 | `versions/v1/prediction.zip` |
| v2 | 2026-06-16 | 只改推荐；复用 v1 分类；测试分布加权验证；长度感知/用户冷启/rank 融合候选；最终选择加性权重调优 | 0.710287 | 0.560433 | 0.635360 | 0.6939 | 0.4701 | 0.5820 | 历史版本 | `versions/v2/prediction.zip` |
| v3 | 2026-06-16 | 只改分类；直接 label propagation；无传播节点回退多数类；推荐复用 v2 | 0.733737 | 0.560433 | 0.647085 | 0.7485 | 0.4701 | 0.6093 | 历史版本 | `versions/v3/prediction.zip` |
| v4 | 2026-06-16 | 只改推荐；无历史用户按 `u_cat_01+u_cat_02` 分组热度冷启动；分类复用 v3 | 0.733737 | 0.560433 | 0.647085 | 0.7485 | 0.4828 | 0.6156 | 历史版本 | `versions/v4/prediction.zip` |
| v5 | 2026-06-16 | 分类零传播节点用 Ridge 属性模型回退；推荐有历史用户强化 repeat、降低 transition | 0.742057 | 0.564636 | 0.653347 | 0.7539 | 0.4853 | 0.6196 | 历史版本 | `versions/v5/prediction.zip` |
| v6 | 2026-06-16 | 只改推荐；max_hist 扩到 120；短历史用户画像 prior；分类复用 v5 | 0.742057 | 0.568716 | 0.655387 | 0.7539 | 0.4878 | 0.6208 | 历史版本 | `versions/v6/prediction.zip` |
| v7 | 2026-06-16 | 只改分类；fallback Ridge `alpha=0.4`；推荐复用 v6 | 0.746596 | 0.568716 | 0.657656 | 0.7586 | 0.4878 | 0.6232 | 历史版本 | `versions/v7/prediction.zip` |
| v8 | 2026-06-16 | 只改推荐；零历史用户 additive 画像融合；分类复用 v7 | 0.746596 | 0.568716 | 0.657656 | 0.7586 | 0.4891 | 0.6239 | 历史官方最佳 | `versions/v8/prediction.zip` |
| v9 | 2026-06-16 | 只改推荐；零历史阈值 m10；seq_len=2 专用排序；分类复用 v8 | 0.746596 | 0.568777 | 0.657686 | - | - | - | 保守待验证 | `versions/v9/prediction.zip` |
| v10 | 2026-06-16 | 只改推荐；零历史阈值 m12；短历史 prior 权重 0.08/阈值 30；分类复用 v9 | 0.746596 | 0.568782 | 0.657689 | 0.7586 | 0.4891 | 0.6239 | 未提升 | `versions/v10/prediction.zip` |
| v11 | 2026-06-16 | 只改分类；LP 类别先验 `beta=-0.25` 校准；推荐复用官方最佳 v8 | 0.747352 | 0.568716 | 0.658034 | - | - | - | 内部分类最佳/待官方验证 | `versions/v11/prediction.zip` |
| v12 | 2026-06-17 | 只改推荐零历史用户；PyTorch 用户特征神经塔 + v8 规则排序 rank 融合；分类复用 v11 | 0.747352 | 0.568716 | 0.658034 | 0.7579 | 0.4926 | 0.6253 | 历史官方最佳 | `versions/v12/prediction.zip` |
| v13 | 2026-06-17 | 官方最佳分类 v8 + 官方最佳推荐 v12 重组；不重训模型 | 0.746596 | 0.568716 | 0.657656 | 预计 0.7586 | 预计 0.4926 | 预计 0.6256 | 理论重组候选/待官方验证 | `versions/v13/prediction.zip` |
| v14 | 2026-06-17 | 推荐零历史 3seed 神经塔 ensemble；分类复用 v8 | 0.746596 | 0.568716 | 0.657656 | 0.7586 | 0.4942 | 0.6264 | 历史官方最佳 | `versions/v14/prediction.zip` |
| v15 | 2026-06-17 | 推荐零历史沿用 v14；短历史 `seq_len=1/2/3` 新增用户特征+短序列神经塔融合；分类复用 v8 | 0.746596 | 0.569711 | 0.658154 | 0.7586 | 0.4923 | 0.6255 | 线上失败/不再提交 | `versions/v15/prediction.zip` |
| v16 | 2026-06-17 | 推荐只改零历史；dropout35 三 seed 稳健融合；移除 v15 短历史神经塔；分类复用 v8 | 0.746596 | 0.568716 | 0.657656 | 0.7586 | 0.4944 | 0.6265 | 历史官方最佳 | `versions/v16/prediction.zip` |
| v17 | 2026-06-17 | 推荐 v16 零历史 + 短历史保守神经 rerank；top1 不变；分类复用 v8 | 0.746596 | 0.568907 | 0.657752 | 0.7586 | 0.4947 | 0.6266 | 当前官方最佳 | `versions/v17/prediction.zip` |

### 版本详细记录

#### v1

版本定位：

- 基于本地 `A分类` / `A推荐` 的 A 榜初版可提交方案。
- 目标是先形成稳定、可复现、提交格式正确的强 baseline。

代码与工程改动：

- 新建 `our_solution/`，与 baseline 源码隔离。
- 默认读取根目录本地数据：
  - `A分类/A1.npz`
  - `A分类/sample_submission.csv`
  - `A推荐/`
- 主入口为 `our_solution/run.py`。
- 新增可选 Qwen 客户端 `our_solution/src/qwen_client.py`，可读取 `.env`，但预测主流程默认不依赖 API。
- 增加自动归档规则：
  - 根目录保留最新版 `prediction.zip`
  - 版本目录保留 `versions/v1/prediction.zip`
  - 版本目录保留代码快照、CSV、trajectory 和 summary

模型与策略改动：

- 分类任务：
  - 使用 CSR 稀疏矩阵读取图和特征。
  - 构造原始特征、图平滑特征、label propagation 特征和度统计特征。
  - 串行验证 Ridge / SGD / LogisticRegression。
  - 最佳配置：`logreg_sparse_graph`。
- 推荐任务：
  - 不使用深度 GRU/SASRec，改为轻量混合排序器。
  - 搜索全局热度、历史 item 转移、重复购买、用户特征、item 特征等权重。
  - 默认不排除历史 item。
  - 最佳配置：`raw_transition_repeat`。

验证效果：

- 分类内部验证 Accuracy：`0.710287443267776`
- 推荐内部验证 NDCG@10：`0.5572047667149482`
- 内部综合分：`0.6337461049913621`
- 官方 A 榜提交时间：`2026-06-16 04:41:43`
- 官方 A 榜分类分数：`0.6939`
- 官方 A 榜推荐分数：`0.4687`
- 官方 A 榜总分：`0.5813`
- `A1.csv` 行数：`2751`
- `A2.csv` 行数：`10000`
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

结论：

- v1 曾为官方 A 榜最佳版本，已被 v2 小幅超过。
- 推荐任务线上分低于内部验证较多，后续 v2 优先改进推荐验证策略和泛化。
- 如需回滚到 v1，请使用 `versions/v1/prediction.zip`。

#### v2

版本定位：

- A 榜推荐提分候选版本。
- 分类不重训，直接复用 v1 的 `A1.csv`，降低分类分数回退风险。
- 推荐任务改为按测试历史长度分布进行验证和模型选择。

代码与工程改动：

- `our_solution/run.py` 新增 `--reuse_a1` / `--reuse_a2`。
- 每次运行会清理旧输出，最终提交包必须同时包含 `A1.csv` 和 `A2.csv`。
- `versions/VERSION_INDEX.json` 记录推荐测试分布加权 NDCG 和复用任务标记。
- `our_solution/src/recommendation.py` 新增：
  - `test_like_recommendation_split`
  - `masked_history_eval`
  - `LengthAwareHybridRecommender`
  - `UserFeaturePopularity`
  - `rank_fusion_topk`
- v2 运行命令：

```powershell
python .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v1\submission\A1.csv `
  --budget 8 `
  --seed 42 `
  --version v2 `
  --note "推荐长度感知+冷启动用户特征+rank融合；加性权重按测试分布验证调优"
```

模型与策略改动：

- 分类：
  - 复用 v1 预测文件，不改变分类模型。
- 推荐：
  - 使用测试集历史长度分布做 masked validation。
  - 测试长度分布权重：`<=3: 0.8992`，`4-10: 0.0061`，`11-30: 0.0149`，`31-80: 0.0242`，`>80: 0.0556`。
  - 新增长度感知、用户特征冷启动、rank 融合候选。
  - 离线选择出的最佳提交配置为 `v2_weight_tuned_repeat_heavy`：
    - `pop_weight=0.6`
    - `transition_weight=0.6`
    - `repeat_weight=0.65`
    - `item_feature_weight=0.0`
    - `max_hist=50`
    - `decay=0.94`

验证效果：

- 分类内部验证 Accuracy：`0.710287443267776`（复用 v1）
- 推荐普通内部验证 NDCG@10：`0.560432987176212`
- 推荐 masked validation NDCG@10：`0.4555699674182959`
- 推荐测试分布加权 NDCG@10：`0.4557473176127268`
- v1 reference 测试分布加权 NDCG@10：`0.4547633176443783`
- 内部综合分：`0.635360215221994`
- 分段 NDCG@10：
  - `<=3`: `0.4442135236338184`
  - `4-10`: `0.5027574741383186`
  - `11-30`: `0.5829233877786618`
  - `31-80`: `0.5867130439784696`
  - `>80`: `0.5460374537189299`
- 官方 A 榜提交时间：`2026-06-16 12:30:27`
- 官方 A 榜分类分数：`0.6939`
- 官方 A 榜推荐分数：`0.4701`
- 官方 A 榜总分：`0.5820`
- 官方 A 榜排名：`61`
- `A1.csv` 行数：`2751`，与 v1 完全一致。
- `A2.csv` 行数：`10000`。
- `A2.csv` 相对 v1 有 `2244` 行推荐发生变化。
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v2/prediction.zip`

结论：

- v2 曾为官方 A 榜最佳版本，已被 v3 明显超过。
- 推荐线上分相对 v1 只提升 `0.0014`，说明 v2 推荐调参方向有效但收益很小。
- 当前和榜首相比，分类差距更大，v3 应优先提升分类，同时继续优化推荐短历史/冷启动。

#### v3

版本定位：

- A 榜分类提分候选版本。
- 推荐不重算，直接复用 v2 的 `A2.csv`，避免推荐分回退。
- 分类从“LP 作为特征 + 线性模型”改成“直接 label propagation 输出标签”。

代码与工程改动：

- `our_solution/src/classification.py` 新增：
  - `label_propagation_scores`
  - `predict_label_propagation`
- `candidate_configs()` 中新增 direct label propagation 候选：
  - `label_prop_direct_5_095_prior`
  - `label_prop_direct_3_099_prior`
  - `label_prop_direct_30_099_prior`
  - `label_prop_direct_20_085_prior`
- v3 运行命令：

```powershell
python .\our_solution\run.py `
  --task 1 `
  --reuse_a2 .\versions\v2\submission\A2.csv `
  --budget 10 `
  --seed 42 `
  --version v3 `
  --note "分类改为直接label propagation；推荐复用v2"
```

模型与策略改动：

- 分类：
  - 使用对称归一化邻接矩阵，带自环。
  - 在训练标签上执行 direct label propagation。
  - 验证最佳配置：`lp_steps=5`，`lp_alpha=0.95`。
  - 对完全收不到标签传播分数的节点，回退为训练集多数类 `4`。
- 推荐：
  - 复用 v2 的 `A2.csv`，不改变推荐结果。

验证效果：

- 分类内部验证 Accuracy：`0.7337367624810892`
- v2 分类内部验证 Accuracy：`0.710287443267776`
- 分类内部提升：`+0.0234493192133132`
- 推荐内部 NDCG@10：`0.560432987176212`（复用 v2）
- 推荐测试分布加权 NDCG@10：`0.4557473176127268`（复用 v2）
- 内部综合分：`0.6470848748286506`
- final stats：
  - `zero_score_nodes`: `593`
  - `fallback_label`: `4`
  - `mean_confidence`: `0.6825249791145325`
- `A1.csv` 行数：`2751`
- `A1.csv` 相对 v2 有 `649` 行标签发生变化。
- `A2.csv` 与 v2 完全一致。
- 官方 A 榜提交时间：`2026-06-16 13:24:59`
- 官方 A 榜分类分数：`0.7485`
- 官方 A 榜推荐分数：`0.4701`
- 官方 A 榜总分：`0.6093`
- 官方 A 榜排名：`45`
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v3/prediction.zip`
- 上一版官方结果备份：`versions/v2/prediction.zip`

结论：

- v3 曾为官方 A 榜最佳版本，已被 v4 超过。
- direct label propagation 对分类任务有效，分类线上从 v2 的 `0.6939` 提升到 `0.7485`。
- 当前距榜首的推荐差距 `0.03629` 大于分类差距 `0.01559`，v4 应重点转向推荐任务。

#### v4

版本定位：

- A 榜推荐提分候选版本。
- 分类不重算，直接复用 v3 的 `A1.csv`。
- 推荐只改测试中 `3515` 个无历史用户；有历史用户完全沿用 v3/v2 推荐结果。

代码与工程改动：

- `our_solution/src/recommendation.py` 新增 `ZeroUserGroupHybridRecommender`。
- 新增推荐候选配置 `v4_zero_user_group_v2_nonzero`。
- 低风险复用：
  - `A1.csv` 与 v3 完全一致。
  - `A2.csv` 只改变无历史用户，变化行数 `3515`。
- v4 运行命令：

```powershell
python .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v3\submission\A1.csv `
  --budget 11 `
  --seed 42 `
  --version v4 `
  --note "推荐无历史用户按u_cat_01+u_cat_02分组热度冷启动；分类复用v3"
```

模型与策略改动：

- 分类：
  - 复用 v3 的 `A1.csv`，不改变分类结果。
- 推荐：
  - `seq_len == 0`：使用用户特征分组热度。
    - 主分组：`u_cat_01 + u_cat_02`
    - 最小组样本数：`3`
    - 低频回退：`u_cat_01`
    - 再回退：全局 target 热度
  - `seq_len > 0`：保持 v2 的 `v2_weight_tuned_repeat_heavy`。

验证效果：

- 分类内部验证 Accuracy：`0.7337367624810892`（复用 v3）
- 推荐普通内部验证 NDCG@10：`0.560432987176212`
- 推荐 masked validation NDCG@10：`0.4712679402624184`
- 推荐测试分布加权 NDCG@10：`0.47136855782306614`
- v3/v2 推荐测试分布加权 NDCG@10：`0.4557473176127268`
- 测试分布加权提升：`+0.01562124021033934`
- 分段 NDCG@10：
  - `<=3`: `0.4615858993125766`
  - `4-10`: `0.5027574741383186`
  - `11-30`: `0.5829233877786618`
  - `31-80`: `0.5867130439784696`
  - `>80`: `0.5460374537189299`
- `A1.csv` 行数：`2751`，与 v3 完全一致。
- `A2.csv` 行数：`10000`。
- `A2.csv` 相对 v3 有 `3515` 行推荐发生变化，全部为无历史用户。
- 官方 A 榜提交时间：`2026-06-16 13:46:20`
- 官方 A 榜分类分数：`0.7485`
- 官方 A 榜推荐分数：`0.4828`
- 官方 A 榜总分：`0.6156`
- 官方 A 榜排名：`37`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v4/prediction.zip`
- 上一版官方结果备份：`versions/v3/prediction.zip`

结论：

- v4 曾是官方 A 榜最佳版本，已被 v5 超过。
- 无历史用户冷启动对线上推荐有效，推荐从 v3 的 `0.4701` 提升到 `0.4828`。
- v4 当时距榜首的推荐差距 `0.02359` 大于分类差距 `0.01559`，因此 v5 同时推进推荐和分类。

#### v5

版本定位：

- A 榜内部提分候选版本。
- 分类和推荐都重算，不复用 v4 文件。
- 官方 A 榜已验证有效，曾替代 v4 成为官方最佳版本；现已被后续版本超过。

代码与工程改动：

- `our_solution/src/classification.py` 新增 `label_prop_fallback_model` 流程。
- 分类候选新增 `label_prop_zero_ridge_attr_1hop_nobal`。
- `our_solution/src/recommendation.py` 新增推荐候选 `v5_zero_group_repeat100_trans035`。
- v5 运行命令：

```powershell
python .\our_solution\run.py `
  --budget 5 `
  --seed 42 `
  --version v5 `
  --note "分类零传播节点Ridge回退+推荐有历史用户repeat强化"
```

模型与策略改动：

- 分类：
  - 保留 v3 的 direct label propagation 主链路。
  - 对 `593` 个测试集零传播分数节点，不再统一回退多数类 `4`。
  - 使用无类别均衡的 Ridge 模型，基于原始属性、1-hop 平滑属性和度特征预测这些零传播节点。
- 推荐：
  - 继续保留 v4 的无历史用户 `u_cat_01+u_cat_02` 分组热度冷启动。
  - 对有历史用户使用更强 repeat 权重：
    - `pop_weight=0.7`
    - `transition_weight=0.35`
    - `repeat_weight=1.0`
    - `max_hist=80`
    - `decay=0.94`

验证效果：

- 分类内部验证 Accuracy：`0.7420574886535553`
- 推荐普通内部验证 NDCG@10：`0.564635627840771`
- 推荐 masked validation NDCG@10：`0.4719755657777171`
- 推荐测试分布加权 NDCG@10：`0.4722514037837439`
- 内部综合分：`0.6533465582471631`
- `A1.csv` 行数：`2751`
- `A1.csv` 相对 v4 有 `89` 行标签发生变化。
- `A2.csv` 行数：`10000`
- `A2.csv` 相对 v4 有 `3027` 行推荐发生变化，全部为有历史用户。
- 官方 A 榜提交时间：`2026-06-16 14:30:22`
- 官方 A 榜分类分数：`0.7539`
- 官方 A 榜推荐分数：`0.4853`
- 官方 A 榜总分：`0.6196`
- 官方 A 榜排名：`33`
- 相比 v4：总分 `+0.0040`，分类 `+0.0054`，推荐 `+0.0025`，排名提升 `4` 名。
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v5/prediction.zip`
- 已验证官方最佳备份：`versions/v5/prediction.zip`

结论：

- v5 曾是官方 A 榜已验证最佳版本，已被 v6 超过。
- 分类零传播节点回退与推荐 repeat 强化在线上都贡献了正收益。
- 当前距榜首推荐差距 `0.02109` 大于分类差距 `0.01019`，下一版仍优先围绕推荐提分。

#### v6

版本定位：

- A 榜推荐提分候选版本。
- 分类不重算，直接复用 v5 的 `A1.csv`。
- 官方 A 榜已验证有效，曾替代 v5 成为官方最佳版本；现已被 v7 超过。

代码与工程改动：

- `our_solution/src/recommendation.py` 为 `HybridRecommender` 新增分数级接口 `score_row`。
- 新增 `ShortHistoryUserPriorHybridRecommender`：
  - `seq_len == 0`：完全复用 v5 无历史用户冷启动。
  - `seq_len in {1,2,3}`：在 v5 分数上加入弱用户画像 prior。
  - `seq_len > 3`：主要沿用基础热度、转移、repeat 排序。
- 新增推荐候选 `v6_weight_probe_pop055_trans01_rep11`。
- v6 运行命令：

```powershell
python .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v5\submission\A1.csv `
  --budget 12 `
  --seed 42 `
  --version v6 `
  --note "推荐max_hist扩展+短历史用户画像prior；分类复用v5"
```

模型与策略改动：

- 分类：
  - 复用 v5 的 `A1.csv`，不改变分类结果。
- 推荐：
  - 无历史用户：继续使用 `u_cat_01+u_cat_02` 分组热度冷启动。
  - 短历史用户：基于 `u_cat_01+u_cat_02`、`u_cat_01+u_cat_06`、`u_cat_02+u_cat_06` 的 target 热度加入弱 prior。
  - 有历史基础配置：
    - `pop_weight=0.55`
    - `transition_weight=0.1`
    - `repeat_weight=1.1`
    - `max_hist=120`
    - `decay=0.94`

验证效果：

- 分类内部验证 Accuracy：`0.7420574886535553`（复用 v5）
- 推荐普通内部验证 NDCG@10：`0.5687163621011415`
- 推荐 masked validation NDCG@10：`0.47336033288519846`
- 推荐测试分布加权 NDCG@10：`0.47377867858432454`
- v5 推荐测试分布加权 NDCG@10：`0.4722514037837439`
- 测试分布加权提升：`+0.0015272748005806247`
- 内部综合分：`0.6553869253773483`
- 多 seed 对照：v6 相比 v5 平均提升 `+0.000930`，5/5 个 seed 为正。
- 分段 NDCG@10：
  - `<=3`: `0.4632445017793992`
  - `4-10`: `0.48082351400911977`
  - `11-30`: `0.5955381360675142`
  - `31-80`: `0.5898985105082103`
  - `>80`: `0.560200305164182`
- `A1.csv` 行数：`2751`，与 v5 完全一致。
- `A2.csv` 行数：`10000`
- `A2.csv` 相对 v5 有 `3530` 行推荐发生变化：
  - `seq_len=0`: `0`
  - `seq_len=1-3`: `3021`
  - `seq_len=4-10`: `19`
  - `seq_len>10`: `490`
- 官方 A 榜提交时间：`2026-06-16 15:20:53`
- 官方 A 榜分类分数：`0.7539`
- 官方 A 榜推荐分数：`0.4878`
- 官方 A 榜总分：`0.6208`
- 官方 A 榜排名：`33`
- 相比 v5：总分 `+0.0012`，分类不变，推荐 `+0.0025`，排名不变。
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v6/prediction.zip`
- 已验证官方最佳备份：`versions/v6/prediction.zip`

结论：

- v6 曾是官方 A 榜已验证最佳版本，现已被 v7 超过。
- v6 推荐线上从 v5 的 `0.4853` 提升到 `0.4878`，说明短历史用户画像 prior 与低 transition 配置有效。
- v6 的推荐结果被 v7 复用；当前线上最佳推荐结果已更新为 v8。

#### v7

版本定位：

- A 榜分类低风险提分候选版本。
- 推荐不重算，直接复用 v6 的 `A2.csv`。
- 官方 A 榜已验证有效，曾是官方 A 榜和内部验证最佳版本；现已被 v8 超过。

代码与工程改动：

- `our_solution/src/classification.py` 前置新增候选 `v7_label_prop_zero_ridge_attr_1hop_alpha04_nobal`。
- 保留 v5 的 `label_prop_zero_ridge_attr_1hop_nobal` 作为对照候选。
- v7 运行命令：

```powershell
python .\our_solution\run.py `
  --task 1 `
  --reuse_a2 .\versions\v6\submission\A2.csv `
  --budget 6 `
  --seed 42 `
  --version v7 `
  --note "分类fallback Ridge alpha=0.4；推荐复用v6"
```

模型与策略改动：

- 分类：
  - 主体仍为 v5 的 label propagation。
  - 只调整零传播节点 fallback 模型：Ridge `alpha` 从 `1.0` 改为 `0.4`。
  - fallback 特征保持 `attr + 1hop smooth attr + degree`，`class_weight=None`。
- 推荐：
  - 完全复用 v6 的 `A2.csv`。

验证效果：

- 分类内部验证 Accuracy：`0.7465960665658093`
- v6/v5 分类内部验证 Accuracy：`0.7420574886535553`
- 分类内部提升：`+0.004538577912254`
- 推荐普通内部验证 NDCG@10：`0.5687163621011415`（复用 v6）
- 推荐测试分布加权 NDCG@10：`0.47377867858432454`（复用 v6）
- 内部综合分：`0.6576562143334754`
- 分类多 seed 探针：
  - v7 fallback Ridge `alpha=0.4`，8 seed 平均 Accuracy：`0.747636`
  - v5/v6 fallback Ridge `alpha=1.0`，8 seed 平均 Accuracy：`0.745651`
- `A1.csv` 行数：`2751`
- `A1.csv` 相对 v6 变化行数：`86`
- `A2.csv` 行数：`10000`
- `A2.csv` 与 v6 完全一致。
- `A2.csv` 每行推荐 item 数为 `10`，无重复 item，item 全部来自 `A推荐/item.csv`。
- 官方 A 榜提交时间：`2026-06-16 16:23:08`
- 官方 A 榜分类分数：`0.7586`
- 官方 A 榜推荐分数：`0.4878`
- 官方 A 榜总分：`0.6232`
- 官方 A 榜排名：`29`
- 相比 v6：总分 `+0.0024`，分类 `+0.0047`，推荐不变，排名提升 `4` 名。
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v7/prediction.zip`
- 已验证官方最佳备份：`versions/v7/prediction.zip`

结论：

- v7 曾替代 v6 成为官方 A 榜最佳版本，现已被 v8 超过。
- 分类 fallback Ridge `alpha=0.4` 的线上收益成立；下一版重点回到推荐，因为 v7 距榜首推荐差距 `0.01859` 大于分类差距 `0.00549`。

#### v8

版本定位：

- A 榜推荐提分候选版本。
- 分类不重算，直接复用 v7 的 `A1.csv`。
- 官方 A 榜已验证有效，曾是官方 A 榜已验证最佳版本；现已被 v12 超过。

代码与工程改动：

- `our_solution/src/recommendation.py` 扩展 `ShortHistoryUserPriorHybridRecommender`：
  - 新增零历史用户 additive 画像融合。
  - 新增 `zero_additive_specs` / `zero_additive_weights` / `zero_additive_global_weight` 等配置项。
- 新增推荐候选 `v8_zero_additive_user_fusion`。
- v8 运行命令：

```powershell
python .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v7\submission\A1.csv `
  --budget 9 `
  --seed 42 `
  --version v8 `
  --note "推荐零历史用户additive画像融合；分类复用v7"
```

模型与策略改动：

- 分类：
  - 复用 v7 的 `A1.csv`。
- 推荐：
  - 非零历史用户完全沿用 v6/v7 的推荐逻辑。
  - 零历史用户从 v6/v7 的单一路径分组 top item，改为多路画像 additive 融合：
    - `u_cat_01 + u_cat_02`，权重 `0.25`
    - `u_cat_01 + u_cat_02 + u_cat_06`，权重 `0.35`
    - `u_cat_01`，权重 `0.25`
    - 全局热度弱 prior，权重 `0.4`
    - 最小分组样本数 `20`

验证效果：

- 分类内部验证 Accuracy：`0.7465960665658093`（复用 v7）
- 推荐普通内部验证 NDCG@10：`0.5687163621011415`
- 推荐 masked validation NDCG@10：`0.47431420722635514`
- 推荐测试分布加权 NDCG@10：`0.47472789033043017`
- v7/v6 推荐测试分布加权 NDCG@10：`0.47377867858432454`
- 测试分布加权提升：`+0.0009492117461056271`
- 内部综合分：`0.6576562143334754`
- 多 seed 对照：v8 相比 v7/v6 推荐测试分布加权平均提升 `+0.000821`，5 个 seed 中 4 个为正。
- 分段 NDCG@10：
  - `<=3`: `0.46430011982444547`
  - `4-10`: `0.48082351400911977`
  - `11-30`: `0.5955381360675142`
  - `31-80`: `0.5898985105082103`
  - `>80`: `0.560200305164182`
- `A1.csv` 行数：`2751`，与 v7 完全一致。
- `A2.csv` 行数：`10000`
- `A2.csv` 相对 v7 有 `3424` 行推荐发生变化，全部为零历史用户。
- `A2.csv` 每行推荐 item 数为 `10`，无重复 item，item 全部来自 `A推荐/item.csv`。
- 官方 A 榜提交时间：`2026-06-16 17:00:51`
- 官方 A 榜分类分数：`0.7586`
- 官方 A 榜推荐分数：`0.4891`
- 官方 A 榜总分：`0.6239`
- 官方 A 榜排名：`30`
- 相比 v7：总分 `+0.0007`，分类不变，推荐 `+0.0013`，排名下降 `1` 名。
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v8/prediction.zip`
- 已验证官方最佳备份：`versions/v8/prediction.zip`

结论：

- v8 曾是官方 A 榜已验证最佳版本，现已被 v12 超过。
- 零历史用户 additive 画像融合在线上有效；后续继续围绕冷启动和短历史用户做精调。

#### v9

版本定位：

- A 榜推荐微调候选版本。
- 分类不重算，直接复用 v8 的 `A1.csv`。
- 相对 v8 改动较小，是更保守的推荐待验证版本；内部推荐测试分布加权最佳已更新为 v12。

代码与工程改动：

- `our_solution/src/recommendation.py` 扩展 `ShortHistoryUserPriorHybridRecommender`：
  - 新增 `length_base_configs` / `length_base_models`，允许按测试历史长度覆写基础推荐器。
  - `predict_row` 中先根据 `raw_len` 选择专用基础模型，再叠加短历史用户画像 prior。
- 新增推荐候选 `v9_zero_m10_len2_segment`。
- v9 运行命令：

```powershell
python .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v8\submission\A1.csv `
  --budget 10 `
  --seed 42 `
  --version v9 `
  --note "推荐零历史阈值m10+seq_len2专用排序；分类复用v8"
```

模型与策略改动：

- 分类：
  - 复用 v8/v7 的 `A1.csv`。
- 推荐：
  - 以 v8 的零历史 additive 画像融合为基础。
  - 零历史用户 additive 融合的最小分组样本数从 `20` 下调为 `10`。
  - 对 `seq_len=2` 用户使用专用基础排序：
    - `pop_weight=1.1`
    - `transition_weight=0.05`
    - `repeat_weight=0.0`
    - `max_hist=3`
    - `decay=0.94`
  - 其他长度用户沿用 v8/v6 基础配置。

验证效果：

- 分类内部验证 Accuracy：`0.7465960665658093`（复用 v8/v7）
- 推荐普通内部验证 NDCG@10：`0.5687767957681257`
- 推荐 masked validation NDCG@10：`0.474354064015775`
- 推荐测试分布加权 NDCG@10：`0.4747675522974703`
- v8 推荐测试分布加权 NDCG@10：`0.47472789033043017`
- 测试分布加权提升：`+0.0000396619670401`
- 内部综合分：`0.6576864311669675`
- 多 seed 探针：v9 相比 v8 推荐测试分布加权平均提升 `+0.000298`，8 个 seed 全部为正。
- 分段 NDCG@10：
  - `<=3`: `0.46434422788387625`
  - `4-10`: `0.48082351400911977`
  - `11-30`: `0.5955381360675142`
  - `31-80`: `0.5898985105082103`
  - `>80`: `0.560200305164182`
- `A1.csv` 行数：`2751`，与 v8 完全一致。
- `A2.csv` 行数：`10000`。
- `A2.csv` 相对 v8 有 `113` 行推荐发生变化：
  - `seq_len=0`: `64`
  - `seq_len=2`: `49`
- `A2.csv` 每行推荐 item 数为 `10`，无重复 item，item 全部来自 `A推荐/item.csv`。
- 官方 A 榜提交时间：`2026-06-17 00:21:40`
- 官方 A 榜分类分数：`0.7579`
- 官方 A 榜推荐分数：`0.4926`
- 官方 A 榜总分：`0.6253`
- 官方 A 榜排名：`27`
- 相比 v8：总分 `+0.0014`，分类 `-0.0007`，推荐 `+0.0035`，排名从 `30` 到 `27`。
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v9/prediction.zip`
- 已验证官方最佳备份：`versions/v8/prediction.zip`

结论：

- v9 相对 v8 的改动很小，A2 只变化 `113` 行，是当前较保守的待验证推荐候选。
- 若 v10 提交风险需要降低，可改用 `versions/v9/prediction.zip`。

#### v10

版本定位：

- A 榜推荐微调候选版本。
- 分类不重算，直接复用 v9 的 `A1.csv`。
- 曾是内部推荐测试分布加权验证最高版本，但相对 v9 的单 seed 提升极小；现已被 v12 超过。

代码与工程改动：

- `our_solution/src/recommendation.py` 新增推荐候选 `v10_zero_m12_short08_config`。
- `candidate_configs()` 将 v10 放在 v9 后，运行 `--budget 11` 时纳入评估。
- v10 运行命令：

```powershell
python .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v9\submission\A1.csv `
  --budget 11 `
  --seed 42 `
  --version v10 `
  --note "推荐零历史m12+短历史prior稳健微调；分类复用v9"
```

模型与策略改动：

- 分类：
  - 复用 v9/v8/v7 的 `A1.csv`。
- 推荐：
  - 以 v9 的零历史 additive 画像融合和 `seq_len=2` 专用排序为基础。
  - 零历史用户 additive 融合的最小分组样本数从 `10` 调整为 `12`。
  - 短历史用户画像 prior 从 `0.05` 调整为 `0.08`。
  - 短历史用户画像 prior 的最小分组样本数从 `20` 调整为 `30`。
  - `seq_len=3` 专用排序探针全部变差，因此没有并入 v10。
  - 分类和长历史推荐逻辑不变。

验证效果：

- 分类内部验证 Accuracy：`0.7465960665658093`（复用 v9/v8/v7）
- 推荐普通内部验证 NDCG@10：`0.5687815879197804`
- 推荐 masked validation NDCG@10：`0.47435436016203886`
- 推荐测试分布加权 NDCG@10：`0.47476784699615343`
- v9 推荐测试分布加权 NDCG@10：`0.4747675522974703`
- 测试分布加权提升：`+0.0000002946986831`
- 内部综合分：`0.6576888272427949`
- 多 seed 探针：v10 参数相对 v9 平均提升约 `+0.000041`，8 个 seed 中 6 个为正；单 seed 42 提升极小。
- 分段 NDCG@10：
  - `<=3`: `0.46434455561817684`
  - `4-10`: `0.48082351400911977`
  - `11-30`: `0.5955381360675142`
  - `31-80`: `0.5898985105082103`
  - `>80`: `0.560200305164182`
- `A1.csv` 行数：`2751`，与 v9 完全一致。
- `A2.csv` 行数：`10000`。
- `A2.csv` 相对 v9 有 `718` 行推荐发生变化：
  - `seq_len=0`: `6`
  - `seq_len=1-3`: `712`
- `A2.csv` 每行推荐 item 数为 `10`，无重复 item，item 全部来自 `A推荐/item.csv`。
- 官方 A 榜提交时间：`2026-06-16 19:17:03`
- 官方 A 榜分类分数：`0.7586`
- 官方 A 榜推荐分数：`0.4891`
- 官方 A 榜总分：`0.6239`
- 官方 A 榜排名：`31`
- 平台反馈/备注：相比 v8 官方分数完全不变，排名下降 `1` 名；v10 的微小离线增益没有转化为线上收益。
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v10/prediction.zip`
- 更保守待验证备份：`versions/v9/prediction.zip`
- 已验证官方最佳备份：`versions/v8/prediction.zip`

结论：

- v10 是当前内部验证最高但不是官方提分版本，官方最佳仍保留 v8。
- 后续提交门槛需要提高：推荐测试分布加权提升至少达到 `+0.001`，或分类内部多 seed 提升达到 `+0.002` 以上，才值得占用提交次数。

#### v11

版本定位：

- A 榜分类提分候选版本。
- 推荐不重算，直接复用官方最佳 v8 的 `A2.csv`。
- 当前是内部综合分最高版本，官方 A 榜仍待提交验证。

代码与工程改动：

- `our_solution/src/classification.py` 的 label propagation 预测流程新增 `class_prior_beta`。
- 新增分类候选 `v11_label_prop_prior_beta_m025_zero_ridge`。
- v11 运行命令：

```powershell
python .\our_solution\run.py `
  --task 1 `
  --reuse_a2 .\versions\v8\submission\A2.csv `
  --budget 2 `
  --seed 42 `
  --version v11 `
  --note "分类LP类别先验beta=-0.25校准；推荐复用官方最佳v8"
```

模型与策略改动：

- 分类：
  - 保留 v7/v8 的 label propagation + 零传播节点 Ridge fallback。
  - 对 LP 分数按训练集类别先验做校准：`score_c *= prior_c ** -0.25`。
  - 该校准会削弱多数类过度优势，给少数类低置信节点更多机会。
- 推荐：
  - 完全复用 v8 的 `A2.csv`，不引入 v10 的无效推荐扰动。

验证效果：

- 分类内部验证 Accuracy：`0.7473524962178517`
- v8/v10 分类内部验证 Accuracy：`0.7465960665658093`
- 分类内部提升：`+0.0007564296520424`
- 推荐普通内部验证 NDCG@10：`0.5687163621011415`（复用 v8）
- 推荐测试分布加权 NDCG@10：`0.47472789033043017`（复用 v8）
- 内部综合分：`0.6580344291594966`
- 分类 20 seed 探针：`beta=-0.25` 相比 v7/v8 平均提升 `+0.00189`，20 个 seed 中 11 个为正。
- `A1.csv` 行数：`2751`
- `A1.csv` 相对 v8 有 `71` 行标签发生变化。
- `A2.csv` 行数：`10000`
- `A2.csv` 与 v8 完全一致。
- `A2.csv` 每行推荐 item 数为 `10`，无重复 item，item 全部来自 `A推荐/item.csv`。
- 官方 A 榜分数：待提交回填。
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v11/prediction.zip`
- 已验证官方最佳备份：`versions/v8/prediction.zip`

结论：

- v11 曾是内部分类最佳候选，现已被 v12 作为最新版继承。
- v11 只改分类且 A1 变化 `71` 行；该分类结果已被 v12 复用。

#### v12

版本定位：

- A 榜深度学习推荐探索版本。
- 分类不重算，直接复用 v11 的 `A1.csv`。
- 推荐只改变 `3515` 个零历史用户，非零历史用户沿用 v8/v11 排序，降低神经模型扰动风险。

代码与工程改动：

- `our_solution/src/recommendation.py` 新增 `NeuralZeroUserFusionRecommender`。
- 新增推荐候选 `v12_neural_zero_user_fusion`。
- PyTorch 依赖采用惰性导入；只有在当前 Python 环境能发现 `torch` 时才把 v12 候选加入搜索。
- v12 使用本地 PyTorch venv 运行：

```powershell
& 'E:\desktop\Artificial-Intelligence\Pytorch\.venv\Scripts\python.exe' .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v11\submission\A1.csv `
  --budget 12 `
  --seed 42 `
  --version v12 `
  --note "推荐零历史用户神经塔融合；分类复用v11"
```

模型与策略改动：

- 分类：
  - 复用 v11 的 LP 类别先验校准结果。
- 推荐：
  - 对零历史用户训练用户特征神经塔，使用 `u_cat_*` 匿名类别特征预测目标 item。
  - 神经塔参数：`emb_dim=16`，`hidden_dim=384`，`dropout=0.25`，`epochs=14`，`batch_size=1024`。
  - 输出时只对 `seq_len == 0` 用户生效。
  - 用 rank 融合合并 v8 零历史规则排序 top100 与神经塔 top50，神经排序权重 `1.2`。
  - 有历史用户完全沿用 v8/v11 结果。

验证效果：

- 分类内部验证 Accuracy：`0.7473524962178517`（复用 v11）
- 推荐普通内部验证 NDCG@10：`0.5687163621011415`
- 推荐 masked validation NDCG@10：`0.47744236771048004`
- 推荐测试分布加权 NDCG@10：`0.4778407601782909`
- v8/v11 推荐测试分布加权 NDCG@10：`0.47472789033043017`
- 测试分布加权提升：`+0.0031128698478607`
- 内部综合分：`0.6580344291594966`
- 分段 NDCG@10：
  - `<=3`: `0.4677619412744685`
  - `4-10`: `0.48082351400911977`
  - `11-30`: `0.5955381360675142`
  - `31-80`: `0.5898985105082103`
  - `>80`: `0.560200305164182`
- `A1.csv` 行数：`2751`，与 v11 完全一致。
- `A2.csv` 行数：`10000`。
- `A2.csv` 相对 v11 有 `3515` 行推荐发生变化，全部为零历史用户。
- `A2.csv` 每行推荐 item 数为 `10`，无重复 item，item 全部来自 `A推荐/item.csv`。
- 官方 A 榜提交时间：`2026-06-17 00:21:40`
- 官方 A 榜分类分数：`0.7579`
- 官方 A 榜推荐分数：`0.4926`
- 官方 A 榜总分：`0.6253`
- 官方 A 榜排名：`27`
- 相比 v8：总分 `+0.0014`，分类 `-0.0007`，推荐 `+0.0035`，排名从 `30` 到 `27`。
- `prediction.zip` 内容：
  - `A1.csv`
  - `A2.csv`

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v12/prediction.zip`
- 当时已验证官方最佳备份：`versions/v12/prediction.zip`；当前官方最佳备份已更新为 `versions/v17/prediction.zip`

结论：

- v12 曾是内部推荐测试分布加权最佳版本和官方 A 榜已验证最佳版本，现已被 v14 超过。
- 该版本首次把 PyTorch 神经模型纳入可提交链路，但只作用于零历史用户，属于低扰动深度学习融合。
- 推荐测试分布加权提升在线上成立；后续优先继续优化推荐，同时评估是否回退到 v8 分类以回收 `0.0007` 分类损失。

#### v13

版本定位：

- A 榜低风险重组候选版本。
- 不重训模型，只组合两个已经在线上验证过的单任务最优输出。
- 目标是回收 v12 相比 v8 的 `0.0007` 分类损失。

代码与工程改动：

- 复用 `versions/v8/submission/A1.csv`。
- 复用 `versions/v12/submission/A2.csv`。
- 重新生成根目录 `prediction.zip` 和 `versions/v13/prediction.zip`。
- `run.py` 当前不支持两个任务都只复用，因此本版本使用同一套归档函数手工生成版本目录。

模型与策略改动：

- 分类：
  - 使用 v8/v7 的分类结果，官方 A 榜分类分数 `0.7586`。
- 推荐：
  - 使用 v12 的零历史用户神经塔融合结果，官方 A 榜推荐分数 `0.4926`。

验证效果：

- 分类内部验证 Accuracy：`0.7465960665658093`（复用 v8）
- 推荐普通内部验证 NDCG@10：`0.5687163621011415`（复用 v12）
- 推荐测试分布加权 NDCG@10：`0.4778407601782909`（复用 v12）
- 内部综合分：`0.6576562143334754`
- 理论 A 榜分类分数：`0.7586`
- 理论 A 榜推荐分数：`0.4926`
- 理论 A 榜总分：`0.6256`
- `A1.csv` 与 v8 完全一致。
- `A2.csv` 与 v12 完全一致。
- 官方 A 榜分数：待提交回填。

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v13/prediction.zip`
- 已验证官方最佳备份：`versions/v12/prediction.zip`

结论：

- v13 曾是最新版提交包，现已被 v16 覆盖。
- 若任务分数独立稳定，v13 预期比 v12 总分高约 `+0.0003`。
- v13 未提交；官方已验证最佳已更新为 v14。

#### v14

版本定位：

- A 榜推荐提分候选版本。
- 分类复用 v8 的 `A1.csv`，使用线上已验证分类分 `0.7586`。
- 推荐只继续改零历史用户，非零历史用户不扰动。

代码与工程改动：

- `our_solution/src/recommendation.py` 扩展 `NeuralZeroUserFusionRecommender`，新增 `ensemble_seeds` 支持。
- 新增候选 `v14_neural_zero_user_3seed_ensemble`。
- v14 运行命令：

```powershell
& 'E:\desktop\Artificial-Intelligence\Pytorch\.venv\Scripts\python.exe' .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v8\submission\A1.csv `
  --budget 13 `
  --seed 42 `
  --version v14 `
  --note "推荐零历史3seed神经塔ensemble；分类复用v8"
```

模型与策略改动：

- 分类：
  - 复用 v8/v7 的分类结果。
- 推荐：
  - 仅对 `seq_len == 0` 的测试用户启用神经模型。
  - 训练 3 个用户特征神经塔，种子为 `42 / 123 / 11`。
  - 每个神经塔取 top20，与 v8 零历史规则排序 top100 做 rank 融合。
  - 融合权重：基础规则 `1.0`，每个神经塔 `0.6`。

验证效果：

- 分类内部验证 Accuracy：`0.7465960665658093`（复用 v8）
- 推荐普通内部验证 NDCG@10：`0.5687163621011415`
- 推荐 masked validation NDCG@10：`0.4787486493259033`
- 推荐测试分布加权 NDCG@10：`0.4791406566107477`
- v12 推荐测试分布加权 NDCG@10：`0.4778407601782909`
- 测试分布加权提升：`+0.0012998964324568`
- 分段 NDCG@10：
  - `<=3`: `0.4692075556344071`
  - `4-10`: `0.48082351400911977`
  - `11-30`: `0.5955381360675142`
  - `31-80`: `0.5898985105082103`
  - `>80`: `0.560200305164182`
- `A1.csv` 与 v8 完全一致。
- `A2.csv` 相对 v12 变化 `3455` 行，全部为零历史用户。
- `A2.csv` 行数 `10000`，每行 `10` 个 item，无重复 item，item 全部来自 `A推荐/item.csv`。
- 官方 A 榜提交时间：`2026-06-17 01:11:49`
- 官方 A 榜分类分数：`0.7586`
- 官方 A 榜推荐分数：`0.4942`
- 官方 A 榜总分：`0.6264`
- 官方 A 榜排名：`25`
- 相比 v12：总分 `+0.0011`，分类 `+0.0007`，推荐 `+0.0016`，排名从 `27` 到 `25`。

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v14/prediction.zip`
- 已验证官方最佳备份：`versions/v14/prediction.zip`

结论：

- v14 曾是官方 A 榜已验证最佳版本，现已被 v16 小幅超过。
- v14 相比 v12 的离线推荐加权提升为 `+0.00130`，比 v13 的理论 `+0.0003` 更值得使用提交次数。
- v14 线上验证成立，后续继续围绕推荐零历史提分；短历史用户改动需要更严格的线上风险约束。

#### v15

版本定位：

- A 榜推荐提分候选版本。
- 分类复用 v8 的 `A1.csv`，继续使用线上已验证分类分 `0.7586`。
- 推荐在 v14 零历史方案基础上，新增短历史用户神经融合，重点覆盖测试集中占比很高的 `seq_len=1/2/3` 用户。

代码与工程改动：

- `our_solution/src/recommendation.py` 扩展 `NeuralZeroUserFusionRecommender`。
- 新增短历史序列塔训练逻辑：用户匿名特征 embedding + 最近最多 3 个 item embedding + 位置 embedding。
- 新增候选 `v15_neural_zero_shortseq_fusion`。
- v15 运行命令：

```powershell
& 'E:\desktop\Artificial-Intelligence\Pytorch\.venv\Scripts\python.exe' .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v8\submission\A1.csv `
  --budget 14 `
  --seed 42 `
  --version v15 `
  --note "推荐零历史v14 ensemble+短历史用户序列神经塔融合；分类复用v8"
```

模型与策略改动：

- 分类：
  - 复用 v8/v7 的分类结果。
- 推荐：
  - `seq_len == 0`：完全沿用 v14 的零历史 3seed 用户特征神经塔 ensemble。
  - `seq_len in {1,2,3}`：使用短历史神经塔 top30，与 v8/v14 规则排序 top100 做 rank 融合。
  - 短历史神经塔参数：seed `42`，训练 `12` epoch，`user_emb=12`，`item_emb=48`，`hidden=512`，`dropout=0.3`。
  - 融合权重：基础规则 `1.0`，短历史神经塔 `0.65`。
  - `seq_len >= 4`：不改变 v14 规则排序。

验证效果：

- 分类内部验证 Accuracy：`0.7465960665658093`（复用 v8）
- 推荐普通内部验证 NDCG@10：`0.569711027361895`
- 推荐 masked validation NDCG@10：`0.48113431968278014`
- 推荐测试分布加权 NDCG@10：`0.48151466566768214`
- v14 推荐测试分布加权 NDCG@10：`0.4791406566107477`
- 测试分布加权提升：`+0.0023740090569344`
- 分段 NDCG@10：
  - `<=3`: `0.47184769026178086`
  - `4-10`: `0.48082351400911977`
  - `11-30`: `0.5955381360675142`
  - `31-80`: `0.5898985105082103`
  - `>80`: `0.560200305164182`
- `A1.csv` 与 v8 完全一致。
- `A2.csv` 相对 v14 变化 `5441` 行：
  - `seq_len=0`: `0/3515`
  - `seq_len=1`: `997/1003`
  - `seq_len=2`: `49/49`
  - `seq_len=3`: `4395/4425`
  - `seq_len>=4`: `0/1008`
- `A2.csv` 行数 `10000`，每行 `10` 个 item，无重复 item，item 全部来自 `A推荐/item.csv`。
- 官方 A 榜提交时间：`2026-06-17 13:58:55`
- 官方 A 榜分类分数：`0.7586`
- 官方 A 榜推荐分数：`0.4923`
- 官方 A 榜总分：`0.6255`
- 官方 A 榜排名：待回填
- 相比 v14：总分 `-0.0009`，分类不变，推荐 `-0.0019`。
- 线上失败原因判断：短历史神经塔对内部 masked validation 过拟合，`seq_len=1/2/3` 的内部构造样本不能代表官方 A 榜真实短历史用户。

提交文件：

- 生成时根目录输出：`prediction.zip`（v15 失败后曾回退到 v14，当前已更新为 v17）
- 版本备份：`versions/v15/prediction.zip`
- 已验证官方最佳备份：`versions/v14/prediction.zip`
- v15 版本备份 SHA256：`4624B9185E90974C0A9C91F37CEB56CA70728035F53E4137C5AB199E9C54157E`
- v15 官方验证失败后，根目录 `prediction.zip` 曾回退到 v14，后续已更新为 v17 候选。

结论：

- v15 是一次失败提交：内部推荐测试分布加权指标提升没有转化到线上。
- 官方 A 榜已验证最佳已更新为 v17；根目录 `prediction.zip` 当前为 v17。
- 后续推荐改动需要降低短历史段大幅替换风险，不能再只按当前 masked validation 选择 `seq_len=1/2/3` 神经融合。

#### v16

版本定位：

- A 榜推荐稳健提分候选版本。
- 分类复用 v8 的 `A1.csv`，继续使用线上已验证分类分 `0.7586`。
- 推荐只改零历史用户，完全移除 v15 失败的短历史神经塔扰动。

代码与工程改动：

- `our_solution/src/recommendation.py` 新增候选 `v16_neural_zero_dropout35_3seed`。
- 默认候选搜索中保留 v12/v14/v16，v15 短历史神经塔因线上失败不再进入默认候选。
- v16 运行命令：

```powershell
& 'E:\desktop\Artificial-Intelligence\Pytorch\.venv\Scripts\python.exe' .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v8\submission\A1.csv `
  --budget 14 `
  --seed 42 `
  --version v16 `
  --note "推荐零历史dropout35三seed稳健融合；移除v15短历史神经塔；分类复用v8"
```

模型与策略改动：

- 分类：
  - 复用 v8/v7 的分类结果。
- 推荐：
  - `seq_len == 0`：用户特征神经塔 dropout 从 `0.25` 提高到 `0.35`。
  - 三个神经塔种子为 `42 / 123 / 314`。
  - 神经 top15 与 v8 零历史规则排序 top100 做 rank 融合，神经 rank 权重 `0.55`。
  - `seq_len >= 1`：完全沿用 v14，不改变短历史和长历史用户。

验证效果：

- 分类内部验证 Accuracy：`0.7465960665658093`（复用 v8）
- 推荐普通内部验证 NDCG@10：`0.5687163621011415`
- 推荐 masked validation NDCG@10：`0.4789407507055668`
- 推荐测试分布加权 NDCG@10：`0.47933181898733324`
- v14 推荐测试分布加权 NDCG@10：`0.4791406566107477`
- 单 split 测试分布加权提升：`+0.0001911623765856`
- 四 split 零历史稳健探针相对 v14-like：
  - 平均提升：`+0.001506`
  - 最小提升：`+0.000191`
  - 各 split：`+0.000191 / +0.002148 / +0.002057 / +0.001628`
- 分段 NDCG@10：
  - `<=3`: `0.4694201472453786`
  - `4-10`: `0.48082351400911977`
  - `11-30`: `0.5955381360675142`
  - `31-80`: `0.5898985105082103`
  - `>80`: `0.560200305164182`
- `A1.csv` 与 v8 完全一致。
- `A2.csv` 相对 v14 变化 `3454` 行，全部为零历史用户；`seq_len=1/2/3` 和 `seq_len>=4` 均不变。
- `A2.csv` 行数 `10000`，每行 `10` 个 item，无重复 item，item 全部来自 `A推荐/item.csv`。
- 根目录 `prediction.zip` 与 `versions/v16/prediction.zip` 哈希一致：`8F94AE50FC646EDDE2D17AE9DEEC467BCA7DBE16B70C98BC9AFC9EF7CEC3299D`

官方 A 榜：

- 提交时间：`2026-06-17 15:17:55`
- 分类分数：`0.7586`
- 推荐分数：`0.4944`
- 总分：`0.6265`
- 排名：`25`
- 平台反馈/备注：相比 v14 总分提升 `+0.0001`，推荐提升 `+0.0002`，分类不变，排名保持 `25`；提升成立但幅度偏小。

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v16/prediction.zip`
- 已验证官方最佳备份：`versions/v17/prediction.zip`

结论：

- v16 曾是官方 A 榜已验证最佳版本，现已被 v17 小幅超过。
- v16 不触碰 v15 失败的短历史用户，只做零历史稳健微调。
- v16 线上只比 v14 小幅提升，后续应提高提交门槛，优先寻找推荐 `+0.001` 量级或分类可验证收益。

#### v17

版本定位：

- A 榜推荐短历史保守 rerank 候选版本。
- 分类复用 v8 的 `A1.csv`，官方 A 榜分类参考分 `0.7586`。
- 推荐沿用 v16 零历史方案，只在 `seq_len=1/2/3` 上做弱短序列神经 rerank。

代码与工程改动：

- `our_solution/src/recommendation.py` 新增候选 `v17_neural_zero_shortseq_conservative`。
- 推荐验证输出新增天然短历史验证指标 `natural_short_val_ndcg@10`。
- v17 运行命令：

```powershell
& 'E:\desktop\Artificial-Intelligence\Pytorch\.venv\Scripts\python.exe' .\our_solution\run.py `
  --task 2 `
  --reuse_a1 .\versions\v8\submission\A1.csv `
  --budget 15 `
  --seed 42 `
  --version v17 `
  --note "推荐v16零历史+短历史保守神经rerank；top1不变；分类复用v8"
```

模型与策略改动：

- 分类：
  - 复用 v8/v7 的分类结果。
- 推荐：
  - `seq_len == 0`：沿用 v16 零历史 dropout35 三 seed 神经塔融合。
  - `seq_len in {1,2,3}`：训练短序列神经塔，但只用弱 rank 权重 `0.08` 和神经 top10。
  - `seq_len >= 4`：不改变 v16 排序。
  - 短序列塔参数：seed `42`，`8` epoch，hidden `384`，dropout `0.45`，lr `0.001`，weight decay `0.0003`。

验证效果：

- 分类内部验证 Accuracy：`0.7465960665658093`（复用 v8）
- 推荐普通内部验证 NDCG@10：`0.568907403498473`
- 推荐 masked validation NDCG@10：`0.4792988636465619`
- 推荐测试分布加权 NDCG@10：`0.4796881814507714`
- v16 推荐测试分布加权 NDCG@10：`0.47933181898733324`
- 单 split 测试分布加权提升：`+0.0003563624634382`
- 天然短历史验证 NDCG@10：`0.3673685817096423`
- v16 天然短历史验证 NDCG@10：`0.35876110541876616`
- 四 split 稳健探针相对 v16：
  - 平均 test-weighted 提升：`+0.000790`
  - 最小 test-weighted 提升：`+0.000356`
  - 平均天然短历史提升：`+0.010901`
  - 最小天然短历史提升：`+0.004773`
- 分段 NDCG@10：
  - `<=3`: `0.4698164578141488`
  - `4-10`: `0.48082351400911977`
  - `11-30`: `0.5955381360675142`
  - `31-80`: `0.5898985105082103`
  - `>80`: `0.560200305164182`
- `A1.csv` 与 v8 完全一致。
- `A2.csv` 相对 v16 变化 `4194` 行：
  - `seq_len=0`: `0/3515`
  - `seq_len=1`: `781/1003`
  - `seq_len=2`: `40/49`
  - `seq_len=3`: `3373/4425`
  - `seq_len>=4`: `0/1008`
- `A2.csv` 相比 v16 的 top1 变化 `0` 行；变化行 top10 overlap 分布：`8: 46`，`9: 2354`，`10: 1794`。
- `A2.csv` 行数 `10000`，每行 `10` 个 item，无重复 item，item 全部来自 `A推荐/item.csv`。
- 根目录 `prediction.zip` 与 `versions/v17/prediction.zip` 哈希一致：`0A9F8B8509F1D824F0F3366BB16150D2937ECDACB85DE0F3A174331F47247FE4`

官方 A 榜：

- 提交时间：`2026-06-17 16:58:13`
- 分类分数：`0.7586`
- 推荐分数：`0.4947`
- 总分：`0.6266`
- 排名：`25`
- 平台反馈/备注：相比 v16 总分提升 `+0.0001`，推荐提升 `+0.0003`，分类不变，排名保持 `25`；提升成立但仍属于边际收益。

提交文件：

- 根目录最新版：`prediction.zip`
- 版本备份：`versions/v17/prediction.zip`
- 已验证官方最佳备份：`versions/v17/prediction.zip`

结论：

- v17 是当前官方 A 榜已验证最佳版本。
- v17 不改零历史和长历史，短历史 top1 不变，只做 top10 内弱 rerank。
- v17 线上只比 v16 小幅提升，后续应继续提高提交门槛，优先寻找推荐 `+0.001` 量级或分类可验证收益。

#### 后续版本记录模板

#### v18_probe

版本定位：

- 未提交实验；用于验证 v18 是否值得覆盖当前官方最佳 v17。
- 分类不动，仍判断为应复用 v8/v17 的 `A1.csv`。
- 推荐只做 probe，不生成新的根目录提交包。

代码与工程改动：

- 未修改主链路源码。
- 新增临时 probe 脚本与结果：
  - `our_solution/output/v18_probe_eval.py`
  - `our_solution/output/v18_multisplit_probe.py`
  - `our_solution/output/v18_combo_probe.py`
  - `our_solution/output/v18_probe_eval_seed42.json`
  - `our_solution/output/v18_multisplit_probe.json`
  - `our_solution/output/v18_combo_probe.json`

验证效果：

- 单 split seed 42 显示短历史 `alpha=0.12/pool=15` 和零历史 5-seed 都有小正收益。
- 多 split 后收益不稳：
  - `probe_short_alpha012_pool15`：mean `+0.0000478`，min `-0.0000825`
  - `v18_zero_5seed_drop35_rank055`：mean `-0.001010`，min `-0.001742`
  - `v18_combo_zero5_seed11_keep_v17short`：mean `-0.0000619`，min `-0.000822`
  - `v18_combo_zero5_seed11_rank045_keep_v17short`：mean `-0.0000795`，min `-0.000945`

提交文件：

- 根目录最新版：`prediction.zip` 仍为 v17
- 官方最佳备份：`versions/v17/prediction.zip`
- v18 probe 未生成可提交 `prediction.zip`

结论：

- v18 probe 不替代当前最佳版本。
- 当前零历史 MLP seed/pool 微调和短历史 alpha 微调都不足以支撑一次提交。
- 下一步需要换更大方向，例如更可靠的零历史特征、item 目标先验、或单独的分类高置信微改 probe。

复制下面模板追加后续版本：

```text
#### vN

版本定位：
- 

代码与工程改动：
- 

模型与策略改动：
- 分类：
- 推荐：

验证效果：
- 分类内部验证 Accuracy：
- 推荐内部验证 NDCG@10：
- 内部综合分：
- 官方 A/B 榜分数：

提交文件：
- 根目录最新版：prediction.zip
- 版本备份：versions/vN/prediction.zip

结论：
- 是否替代当前最佳版本：
- 原因：
```

## 当前方案摘要

分类任务：

- 稀疏 CSR 图数据读取。
- 原始节点特征行归一化。
- 1-hop / 2-hop 图平滑特征。
- direct label propagation 分类候选。
- label propagation 类别先验特征。
- v11 当前使用 LP 类别先验校准 `class_prior_beta=-0.25`。
- Ridge / SGD / LogisticRegression 串行验证搜索。

推荐任务：

- 按 `target_iid` 分层划分内部验证集。
- 增加测试历史长度分布匹配验证和历史截断/置空验证。
- 串行搜索全局热度、历史转移、重复购买、用户匿名特征、item 匿名特征的权重。
- 无历史用户使用 `u_cat_01+u_cat_02` 分组热度冷启动。
- 有历史用户当前使用 v6 的低 transition、强 repeat 配置。
- 短历史用户当前加入 v6 的用户画像弱 prior。
- 零历史用户当前使用 v16 的 dropout35 三 seed PyTorch 用户特征神经塔 ensemble + v8 多路画像 additive 融合。
- v9 新增 `seq_len=2` 专用基础排序，其余有历史用户主要沿用 v6 的低 transition、强 repeat 配置。
- v10 在 v9 基础上微调零历史阈值和短历史用户画像 prior。
- 新增长度感知、用户冷启动、rank 融合候选；当前 v16 最终选择零历史用户多神经塔与规则排序 rank 融合。
- PyTorch GRU4Rec 融合探针已用本地 venv 验证：最佳本地测试分布加权提升约 `+0.00017`，低于提交门槛，暂不进入主提交链路。
- PyTorch 用户特征神经塔已进入主链路，只作用于 `seq_len == 0` 用户；v16 推荐测试分布加权 NDCG@10 达到 `0.479332`。
- 默认不排除历史 item，因为训练集中目标 item 经常已出现在历史序列。

## 注意事项

- 不要直接提交 `our_solution/output/prediction.zip`，除非你确认它是最新输出；提交时优先使用根目录 `prediction.zip`。
- A 榜只需要提交 `prediction.zip`。
- B 榜除预测结果外，还需要提交 `trajectory_B1.json` 和 `trajectory_B2.json`。
- `.env` 中包含 API key，不要复制到公开提交材料中。
